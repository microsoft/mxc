# Main GitHub Actions entry point for PR/CI builds and the upstream of
# `publish.yml`. Each stage lives in its own reusable workflow so this file
# stays a thin orchestrator (mirrors the 1ES `1ES.Build.yml` split).

name: Build

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - 'tests/examples/**'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/PULL_REQUEST_TEMPLATE.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - '**/*.md'
      - '.editorconfig'
  pull_request:
    branches: [main, 'feature/*', 'user/*']
    paths-ignore:
      - 'docs/**'
      - 'tests/examples/**'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/PULL_REQUEST_TEMPLATE.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - '**/*.md'
      - '.editorconfig'
  workflow_call:
  workflow_dispatch:

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  versioning-checks:
    uses: ./.github/workflows/versioning-checks.yml

  lint:
    uses: ./.github/workflows/lint.yml

  windows-build:
    uses: ./.github/workflows/windows-build.yml

  linux-build:
    uses: ./.github/workflows/linux-build.yml

  macos-build:
    uses: ./.github/workflows/macos-build.yml

  sdk-unit-tests:
    uses: ./.github/workflows/sdk-unit-tests.yml

  package-npm-sdk:
    needs: [windows-build, linux-build, macos-build]
    uses: ./.github/workflows/package-npm-sdk.yml

  sdk-integration-tests:
    needs: package-npm-sdk
    uses: ./.github/workflows/sdk-integration-tests.yml
