{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/microsoft/mxc/schemas/dev/mxc-config.schema.0.8.0-dev.json",
  "title": "MXC Configuration",
  "description": "MXC container execution configuration. Defines the recommended config format for both one-shot and state-aware sandbox lifecycle requests. A few deprecated field spellings not listed here are also accepted via serde aliases.",
  "additionalProperties": false,
  "definitions": {
    "BaseProcessUi": {
      "additionalProperties": false,
      "description": "BaseProcessContainer UI isolation settings.",
      "properties": {
        "desktopSystemControl": {
          "description": "Whether desktop system control is allowed.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "ime": {
          "description": "Whether the IME (Input Method Editor) is allowed.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "isolation": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiIsolation"
            },
            {
              "type": "null"
            }
          ],
          "description": "UI isolation level."
        },
        "systemSettings": {
          "description": "System settings access level.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CaptureDenials": {
      "additionalProperties": false,
      "description": "Windows denial-capture settings. The presence of the `captureDenials` object enables capture; all fields are optional.",
      "properties": {
        "mode": {
          "anyOf": [
            {
              "$ref": "#/definitions/CaptureDenialsMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "How each ungranted access check is handled while it is recorded. Both modes log every access the policy does not grant to the ETL trace; the mode only decides whether that access is blocked or allowed. Defaults to `block` when omitted."
        },
        "outputPath": {
          "description": "Absolute path where the denial ETL trace is written. The caller names the path; the OS opens it under the caller's own identity when the trace is sealed. When omitted, MXC writes the trace to a managed per-run temporary file. The parent directory must already exist.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CaptureDenialsMode": {
      "description": "How `captureDenials` handles each ungranted access check while recording it.",
      "oneOf": [
        {
          "description": "`block` — the access stays **denied** and the denial is recorded. Deny-by-default containment is preserved; this is the safe default.",
          "enum": [
            "block"
          ],
          "type": "string"
        },
        {
          "description": "`allow` — the access is **allowed** and recorded (audit mode). This relaxes deny-by-default for the run, so it is a security-sensitive choice and the runner emits a security warning.",
          "enum": [
            "allow"
          ],
          "type": "string"
        }
      ]
    },
    "ClipboardPolicy": {
      "description": "Clipboard access level.",
      "enum": [
        "none",
        "read",
        "write",
        "all"
      ],
      "type": "string"
    },
    "Containment": {
      "description": "Containment backend (abstract intent or concrete backend).",
      "oneOf": [
        {
          "description": "OS-native process sandbox (resolved per host).",
          "enum": [
            "process"
          ],
          "type": "string"
        },
        {
          "description": "Windows AppContainer / BaseContainer.",
          "enum": [
            "processcontainer"
          ],
          "type": "string"
        },
        {
          "description": "VM-class isolation (resolved per host).",
          "enum": [
            "vm"
          ],
          "type": "string"
        },
        {
          "description": "Windows Sandbox (experimental).",
          "enum": [
            "windows_sandbox"
          ],
          "type": "string"
        },
        {
          "description": "Full Linux container.",
          "enum": [
            "lxc"
          ],
          "type": "string"
        },
        {
          "description": "NanVix micro-VM (experimental).",
          "enum": [
            "microvm"
          ],
          "type": "string"
        },
        {
          "description": "Hyperlight micro-VM (experimental).",
          "enum": [
            "hyperlight"
          ],
          "type": "string"
        },
        {
          "description": "WSL container (experimental).",
          "enum": [
            "wslc"
          ],
          "type": "string"
        },
        {
          "description": "macOS Seatbelt.",
          "enum": [
            "seatbelt"
          ],
          "type": "string"
        },
        {
          "description": "Windows IsolationSession (experimental).",
          "enum": [
            "isolation_session"
          ],
          "type": "string"
        },
        {
          "description": "Unprivileged Linux bubblewrap sandbox.",
          "enum": [
            "bubblewrap"
          ],
          "type": "string"
        }
      ]
    },
    "CrossInstanceAccess": {
      "additionalProperties": false,
      "description": "Cross-instance access between separate instances of the same application.",
      "properties": {
        "duplicateHandle": {
          "description": "When false (default), one instance cannot duplicate handles from another instance.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "readVirtualMemory": {
          "description": "When false (default), one instance cannot read another instance's virtual memory.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DebugEntitlement": {
      "additionalProperties": false,
      "description": "Explicit debug entitlement requirements.",
      "properties": {
        "requiredSids": {
          "description": "SIDs that must all be present and enabled on the caller's token (default empty: no specific SIDs required).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "requiredSigningLevel": {
          "anyOf": [
            {
              "$ref": "#/definitions/SigningLevel"
            },
            {
              "type": "null"
            }
          ],
          "description": "Minimum signing level the caller must present (default `none`)."
        }
      },
      "type": "object"
    },
    "DebugProtection": {
      "additionalProperties": false,
      "description": "Controls whether external debuggers may attach to the process.",
      "properties": {
        "allowDebugging": {
          "description": "When false (default), debugger attachment and SeDebugPrivilege-based process manipulation are blocked. When true, anti-debug restrictions are lifted (development/debug builds only).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "entitlement": {
          "anyOf": [
            {
              "$ref": "#/definitions/DebugEntitlement"
            },
            {
              "type": "null"
            }
          ],
          "description": "Explicit entitlement, evaluated only when `requireEntitlement` and `useSpecificEntitlement` are both true."
        },
        "requireEntitlement": {
          "description": "When true, callers seeking otherwise-denied debug-class access must present a matching entitlement (default false: the restriction is absolute).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "useSpecificEntitlement": {
          "description": "Selects which entitlement satisfies `requireEntitlement`. When false (default), the built-in anti-tamper debug entitlement is used and `entitlement` is ignored; when true, the explicit `entitlement` applies.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Experimental": {
      "description": "Experimental features (only honored with `--experimental`). This block is intentionally **permissive** (no `deny_unknown_fields`): experimental backends are in flux, so the schema documents the known shapes for editor help without rejecting in-progress fields. The strict, closed contract is the stable (top-level) surface.",
      "properties": {
        "isolation_session": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationSession"
            },
            {
              "type": "null"
            }
          ],
          "description": "IsolationSession backend config (Windows)."
        },
        "seatbelt": {
          "anyOf": [
            {
              "$ref": "#/definitions/Seatbelt"
            },
            {
              "type": "null"
            }
          ],
          "description": "Seatbelt backend config (pre-promotion alias)."
        },
        "tamperProtection": {
          "anyOf": [
            {
              "$ref": "#/definitions/TamperProtection"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-application tamper-protection policy (Windows). Maps developer intent to per-application mandatory access control and security compartments; only meaningful for a process with a verifiable App Identity."
        },
        "telemetry": {
          "anyOf": [
            {
              "$ref": "#/definitions/Telemetry"
            },
            {
              "type": "null"
            }
          ],
          "description": "Telemetry configuration."
        },
        "test": {
          "anyOf": [
            {
              "$ref": "#/definitions/TestFeature"
            },
            {
              "type": "null"
            }
          ],
          "description": "Placeholder feature for testing experimental infrastructure."
        },
        "windows_sandbox": {
          "anyOf": [
            {
              "$ref": "#/definitions/WindowsSandbox"
            },
            {
              "type": "null"
            }
          ],
          "description": "Windows Sandbox backend config."
        },
        "wslc": {
          "anyOf": [
            {
              "$ref": "#/definitions/Wslc"
            },
            {
              "type": "null"
            }
          ],
          "description": "WSL container backend config."
        }
      },
      "type": "object"
    },
    "Fallback": {
      "additionalProperties": false,
      "description": "AppContainer DACL-mutation fallback policy.",
      "properties": {
        "allowDaclMutation": {
          "description": "Allow the runner to mutate DACLs as a fallback.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Filesystem": {
      "additionalProperties": false,
      "description": "Filesystem access policy.",
      "properties": {
        "deniedPaths": {
          "description": "Paths explicitly denied (override broader allow rules).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "readonlyPaths": {
          "description": "Paths the process can read but not write.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "readwritePaths": {
          "description": "Paths the process can read and write.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "IsolationConfigurationId": {
      "description": "IsolationSession sizing profile.",
      "enum": [
        "small",
        "medium",
        "large",
        "composable"
      ],
      "type": "string"
    },
    "IsolationSession": {
      "description": "IsolationSession backend config. Carries both the one-shot fields (`configurationId`, `user`) and the per-phase state-aware nesting (`provision` / `start` / `stop` / `deprovision`).",
      "properties": {
        "configurationId": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationConfigurationId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Sizing profile (one-shot)."
        },
        "deprovision": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationSessionPhase"
            },
            {
              "type": "null"
            }
          ],
          "description": "State-aware deprovision-phase configuration."
        },
        "provision": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationSessionPhase"
            },
            {
              "type": "null"
            }
          ],
          "description": "State-aware provision-phase configuration."
        },
        "start": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationSessionPhase"
            },
            {
              "type": "null"
            }
          ],
          "description": "State-aware start-phase configuration."
        },
        "stop": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationSessionPhase"
            },
            {
              "type": "null"
            }
          ],
          "description": "State-aware stop-phase configuration."
        },
        "user": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationUser"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional Entra cloud-agent user bundle (one-shot)."
        }
      },
      "type": "object"
    },
    "IsolationSessionPhase": {
      "description": "Per-phase IsolationSession configuration (state-aware lifecycle).",
      "properties": {
        "configurationId": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationConfigurationId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Sizing profile for this phase."
        },
        "user": {
          "anyOf": [
            {
              "$ref": "#/definitions/IsolationUser"
            },
            {
              "type": "null"
            }
          ],
          "description": "Entra cloud-agent user bundle for this phase."
        }
      },
      "type": "object"
    },
    "IsolationUser": {
      "description": "Entra cloud-agent user bundle. Reachable only under the permissive `experimental` surface, so unknown fields are tolerated (forward-compat).",
      "properties": {
        "upn": {
          "description": "User principal name.",
          "type": "string"
        },
        "wamToken": {
          "description": "Short-lived WAM bearer token (passed verbatim to the OS service).",
          "type": "string"
        }
      },
      "required": [
        "upn",
        "wamToken"
      ],
      "type": "object"
    },
    "LaunchMethod": {
      "description": "Seatbelt inner-process launch method.",
      "oneOf": [
        {
          "description": "sandbox_init() + exec (default). Works for third-party GUI apps.",
          "enum": [
            "exec"
          ],
          "type": "string"
        },
        {
          "description": "Launch via macOS LaunchServices (`open`), then apply the sandbox to the inner shell via sandbox-exec. Required for apps with launch constraints.",
          "enum": [
            "open"
          ],
          "type": "string"
        }
      ]
    },
    "Lifecycle": {
      "additionalProperties": false,
      "description": "Container lifecycle settings.",
      "properties": {
        "destroyOnExit": {
          "description": "Destroy the container when the process exits (default true).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "preservePolicy": {
          "description": "Preserve the applied policy after exit (default false).",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Lxc": {
      "additionalProperties": false,
      "description": "LXC container settings.",
      "properties": {
        "distribution": {
          "description": "Distribution image (e.g. `alpine`).",
          "type": [
            "string",
            "null"
          ]
        },
        "release": {
          "description": "Distribution release (e.g. `3.23`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Network": {
      "additionalProperties": false,
      "description": "Network access policy.",
      "properties": {
        "allowLocalNetwork": {
          "description": "Allow binding/listening on local IPs and accepting inbound connections.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allowedHosts": {
          "description": "Hosts explicitly allowed.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "blockedHosts": {
          "description": "Hosts explicitly blocked.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "defaultPolicy": {
          "anyOf": [
            {
              "$ref": "#/definitions/NetworkPolicy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Default outbound policy when no host rule matches."
        },
        "enforcementMode": {
          "anyOf": [
            {
              "$ref": "#/definitions/NetworkEnforcement"
            },
            {
              "type": "null"
            }
          ],
          "description": "How the policy is enforced."
        },
        "proxy": {
          "anyOf": [
            {
              "$ref": "#/definitions/Proxy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Proxy configuration (one of localhost / builtinTestServer / url)."
        }
      },
      "type": "object"
    },
    "NetworkEnforcement": {
      "description": "Network enforcement mechanism.",
      "oneOf": [
        {
          "description": "Per-process capability-based filtering.",
          "enum": [
            "capabilities"
          ],
          "type": "string"
        },
        {
          "description": "Host firewall rules.",
          "enum": [
            "firewall"
          ],
          "type": "string"
        },
        {
          "description": "Both capability and firewall enforcement.",
          "enum": [
            "both"
          ],
          "type": "string"
        }
      ]
    },
    "NetworkPolicy": {
      "description": "Default network policy.",
      "enum": [
        "allow",
        "block"
      ],
      "type": "string"
    },
    "Phase": {
      "description": "State-aware lifecycle phase.",
      "enum": [
        "provision",
        "start",
        "exec",
        "stop",
        "deprovision"
      ],
      "type": "string"
    },
    "PortMapping": {
      "description": "A single host → container port forward. Reachable only under the permissive `experimental` surface, so unknown fields are tolerated (forward-compat).",
      "properties": {
        "containerPort": {
          "description": "Container port.",
          "maximum": 65535.0,
          "minimum": 1.0,
          "type": "integer"
        },
        "protocol": {
          "anyOf": [
            {
              "$ref": "#/definitions/TransportProtocol"
            },
            {
              "type": "null"
            }
          ],
          "description": "Transport protocol for the mapping. Only `tcp` is currently supported."
        },
        "windowsPort": {
          "description": "Host (Windows) port.",
          "maximum": 65535.0,
          "minimum": 1.0,
          "type": "integer"
        }
      },
      "required": [
        "containerPort",
        "windowsPort"
      ],
      "type": "object"
    },
    "Process": {
      "additionalProperties": false,
      "description": "Process execution settings.",
      "properties": {
        "commandLine": {
          "description": "Command line (or script) to execute.",
          "type": [
            "string",
            "null"
          ]
        },
        "cwd": {
          "description": "Working directory for the process.",
          "type": [
            "string",
            "null"
          ]
        },
        "env": {
          "description": "Environment variables as `\"KEY=VALUE\"` strings.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "timeout": {
          "description": "Wall-clock timeout in milliseconds.",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ProcessContainer": {
      "additionalProperties": false,
      "description": "ProcessContainer-specific settings.",
      "properties": {
        "capabilities": {
          "description": "AppContainer capabilities (e.g. `internetClient`, `registryRead`). Each array entry must contain exactly one capability name; commas are rejected because BaseContainer uses commas as its wire delimiter. `learningModeLogging` and `permissiveLearningMode` are reserved and rejected here; use `learningMode`, `--audit`, or the dedicated denial capture configuration instead.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "captureDenials": {
          "anyOf": [
            {
              "$ref": "#/definitions/CaptureDenials"
            },
            {
              "type": "null"
            }
          ],
          "description": "Windows denial capture. When present, the runner records the sandboxed process's access attempts to a learning-mode ETL trace for later inspection. Requires a host that exposes the learning-mode OS API."
        },
        "learningMode": {
          "description": "AppContainer learning mode (deny-and-record): failed access checks are logged for diagnostics while the accesses stay denied; containment is unchanged. Distinct from the allow-all `permissiveLearningMode` capability, which is injected internally by the `--audit` CLI flag or dedicated denial-capture configuration.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "leastPrivilege": {
          "description": "Enforce least-privilege mode.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "ui": {
          "anyOf": [
            {
              "$ref": "#/definitions/BaseProcessUi"
            },
            {
              "type": "null"
            }
          ],
          "description": "BaseProcessContainer UI settings (Windows)."
        }
      },
      "type": "object"
    },
    "ProcessProtection": {
      "additionalProperties": false,
      "description": "Controls process-level isolation and cross-instance access restrictions.",
      "properties": {
        "allowInheritFromAnyIdentity": {
          "description": "When true, a child may inherit the parent's instance regardless of app identity. False (default) restricts inheritance to a matching identity.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "crossInstanceAccess": {
          "anyOf": [
            {
              "$ref": "#/definitions/CrossInstanceAccess"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cross-instance operations permitted between separate instances of the same protected application."
        },
        "neverInheritFromParent": {
          "description": "When true, the process never inherits protection from its parent and always receives its own isolated instance (default false).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "shareInstanceWithChildren": {
          "description": "Parent-side opt-in: when true, this process shares its anti-tamper instance with its children (default false: each child is isolated).",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Proxy": {
      "additionalProperties": false,
      "description": "Proxy configuration. Exactly one variant applies.",
      "properties": {
        "builtinTestServer": {
          "description": "Have wxc launch its own built-in test proxy.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "localhost": {
          "description": "External localhost proxy port.",
          "maximum": 65535.0,
          "minimum": 1.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "url": {
          "description": "Proxy URL (parsed into host:port).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RequireSigning": {
      "additionalProperties": false,
      "description": "Code-signing requirements for the protected process.",
      "properties": {
        "executable": {
          "description": "When true (default), the main executable (or package) must be signed.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "libraries": {
          "description": "When true (default), all loaded DLLs must be signed.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requiredSigningLevel": {
          "anyOf": [
            {
              "$ref": "#/definitions/SigningLevel"
            },
            {
              "type": "null"
            }
          ],
          "description": "Minimum code-signing level required (default `none` — no minimum beyond the presence check implied by `executable`/`libraries`)."
        }
      },
      "type": "object"
    },
    "Seatbelt": {
      "additionalProperties": false,
      "description": "macOS Seatbelt backend configuration.",
      "properties": {
        "extraMachLookups": {
          "description": "Additional Mach service global-names the inner process may resolve.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "guiAccess": {
          "description": "Allow GUI (WindowServer) access.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "keychainAccess": {
          "description": "Allow Keychain access.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "launchMethod": {
          "anyOf": [
            {
              "$ref": "#/definitions/LaunchMethod"
            },
            {
              "type": "null"
            }
          ],
          "description": "Inner process launch method."
        },
        "nestedPty": {
          "description": "Attach the inner process to a nested pty (default true).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "profileOverride": {
          "description": "Replace the generated profile entirely (advanced/testing escape hatch).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SigningLevel": {
      "description": "Minimum code-signing level. A higher level demands a more trusted signer.",
      "oneOf": [
        {
          "description": "No signing-level requirement.",
          "enum": [
            "none"
          ],
          "type": "string"
        },
        {
          "description": "Authenticode-signed.",
          "enum": [
            "authenticode"
          ],
          "type": "string"
        },
        {
          "description": "Microsoft Store signing level.",
          "enum": [
            "store"
          ],
          "type": "string"
        },
        {
          "description": "Microsoft signing level.",
          "enum": [
            "microsoft"
          ],
          "type": "string"
        },
        {
          "description": "Windows signing level.",
          "enum": [
            "windows"
          ],
          "type": "string"
        }
      ]
    },
    "TamperProtection": {
      "additionalProperties": false,
      "description": "Per-application tamper-protection policy (`experimental.tamperProtection`).\n\nUnlike the rest of the permissive `experimental` block, every tamper-protection struct is **closed** (`deny_unknown_fields`). This is a deliberate, selective fail-closed choice: a misspelled protection flag (e.g. `blockUIAccess` for `blockUiAccess`) is a hard config error rather than a silently-dropped field that leaves a protection off while the author believes it is on. It restricts only this security-sensitive section without imposing version-lockstep on the other in-flux experimental backends. See `docs/tamper-protection-policy.md`; the broader \"flip all of `experimental` to closed\" question is tracked separately.\n\nDefault-deny: every omitted field resolves to its most restrictive value, so `\"tamperProtection\": {}` is maximum lockdown. A handful of fields (`enabled`, `requireSigning.executable`, `requireSigning.libraries`) default to `true` because their restrictive value *is* enabled.",
      "properties": {
        "debugProtection": {
          "anyOf": [
            {
              "$ref": "#/definitions/DebugProtection"
            },
            {
              "type": "null"
            }
          ],
          "description": "Debugger-attach controls and the entitlement that permits exceptions."
        },
        "enabled": {
          "description": "Master switch for app protection (default true). When false, the entire section is disabled and no protections are applied.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "processProtection": {
          "anyOf": [
            {
              "$ref": "#/definitions/ProcessProtection"
            },
            {
              "type": "null"
            }
          ],
          "description": "Process-level isolation and cross-instance access restrictions."
        },
        "requireSigning": {
          "anyOf": [
            {
              "$ref": "#/definitions/RequireSigning"
            },
            {
              "type": "null"
            }
          ],
          "description": "Code-signing requirements for the protected process."
        },
        "uiProtection": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiProtection"
            },
            {
              "type": "null"
            }
          ],
          "description": "UI interaction and accessibility across the compartment boundary."
        }
      },
      "type": "object"
    },
    "Telemetry": {
      "description": "Telemetry configuration (`experimental.telemetry`).",
      "properties": {
        "enabled": {
          "description": "Explicit telemetry override. `true` = force on, `false` = force off, omitted = disabled (default off).",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TestFeature": {
      "description": "Placeholder experimental feature.",
      "properties": {
        "message": {
          "description": "Message to log when the feature is applied.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TransportProtocol": {
      "description": "Port-forward transport protocol. Only `tcp` is currently supported by the vendored WSLC SDK runtime; `udp` is rejected at parse time.",
      "enum": [
        "tcp"
      ],
      "type": "string"
    },
    "Ui": {
      "additionalProperties": false,
      "description": "Cross-platform UI isolation policy.",
      "properties": {
        "clipboard": {
          "anyOf": [
            {
              "$ref": "#/definitions/ClipboardPolicy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Clipboard access level."
        },
        "disable": {
          "description": "Disable all UI access (default true).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "injection": {
          "description": "Allow UI injection.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiIsolation": {
      "description": "Desktop UI isolation level.",
      "enum": [
        "desktop",
        "handles",
        "atoms",
        "container"
      ],
      "type": "string"
    },
    "UiProtection": {
      "additionalProperties": false,
      "description": "Controls UI interaction across the compartment boundary. With the exception of `blockUiAccess`, every field is allow-by-exception: false (default) blocks the interaction, true permits it.",
      "properties": {
        "allowExternalHook": {
          "description": "When false (default), external hooks (e.g. `SetWindowsHookEx`) into the protected process are blocked.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allowHandleAccess": {
          "description": "When false (default), access to the USER handles owned by the protected process is blocked.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allowSyntheticInput": {
          "description": "When false (default), injected (synthetic) input targeting the protected process is blocked.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allowWindowMessages": {
          "description": "When false (default), window messages sent to the protected process are blocked (mitigates shatter attacks).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "blockUiAccess": {
          "description": "When true, UIAccess (assistive technology, IMEs) is blocked across the compartment boundary. False (default) permits UIAccess — the one UI field allowed by default.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "WindowsSandbox": {
      "description": "Windows Sandbox backend config.",
      "properties": {
        "daemonPipeName": {
          "description": "Daemon named-pipe override.",
          "type": [
            "string",
            "null"
          ]
        },
        "idleTimeout": {
          "description": "Idle timeout (legacy seconds field).",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "idleTimeoutMs": {
          "description": "Idle timeout before teardown (ms).",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Wslc": {
      "description": "WSL container backend config.",
      "properties": {
        "cpuCount": {
          "description": "vCPU count.",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "gpu": {
          "description": "Enable GPU passthrough.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "image": {
          "description": "Container image reference.",
          "type": [
            "string",
            "null"
          ]
        },
        "imageTarPath": {
          "description": "Path to a local image tarball.",
          "type": [
            "string",
            "null"
          ]
        },
        "memoryMb": {
          "description": "Memory limit (MB).",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "portMappings": {
          "description": "Host → container port forwards. Only TCP is currently supported; the parser rejects `udp` because the WSLC SDK runtime returns `E_NOTIMPL` for UDP port mappings.",
          "items": {
            "$ref": "#/definitions/PortMapping"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "storagePath": {
          "description": "Storage path override.",
          "type": [
            "string",
            "null"
          ]
        },
        "targetOs": {
          "description": "OS inside the WSL container.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    }
  },
  "properties": {
    "$schema": {
      "description": "Optional JSON Schema reference for editor validation. Accepted but ignored by the parser.",
      "type": [
        "string",
        "null"
      ]
    },
    "_comment": {
      "description": "Optional human-readable annotation. Accepted but ignored by the parser."
    },
    "containerId": {
      "description": "Externally assigned container identifier.",
      "type": [
        "string",
        "null"
      ]
    },
    "containment": {
      "anyOf": [
        {
          "$ref": "#/definitions/Containment"
        },
        {
          "type": "null"
        }
      ],
      "description": "Containment backend to use for execution. Accepts abstract intents (`process`, `vm`) and concrete backends; the binary resolves intents to a concrete backend per host at run time."
    },
    "correlationVector": {
      "description": "Microsoft Correlation Vector (MS-CV) seeded at `provision` and returned in the provision result. The client relays it verbatim into every later state-aware phase so all phases of one lifecycle share a telemetry base prefix (emitted under `__TlgCV__`). The executor is the trust boundary: on each non-provision phase it validates the relayed value and *spins* a fresh child element off a mutable base (so multiple invocations of one phase stay distinct), passes an already-frozen vector through unchanged, and reseeds a brand-new base if the relayed value is absent or malformed — so a missing or hostile relay never reaches telemetry unvalidated. Ignored unless experimental telemetry is enabled; not valid on one-shot requests.",
      "type": [
        "string",
        "null"
      ]
    },
    "experimental": {
      "anyOf": [
        {
          "$ref": "#/definitions/Experimental"
        },
        {
          "type": "null"
        }
      ],
      "description": "Experimental features. Only honored when `--experimental` is passed."
    },
    "fallback": {
      "anyOf": [
        {
          "$ref": "#/definitions/Fallback"
        },
        {
          "type": "null"
        }
      ],
      "description": "AppContainer DACL-mutation fallback policy (Windows)."
    },
    "filesystem": {
      "anyOf": [
        {
          "$ref": "#/definitions/Filesystem"
        },
        {
          "type": "null"
        }
      ],
      "description": "Filesystem access policy. Shared across all backends."
    },
    "lifecycle": {
      "anyOf": [
        {
          "$ref": "#/definitions/Lifecycle"
        },
        {
          "type": "null"
        }
      ],
      "description": "Container lifecycle settings."
    },
    "lxc": {
      "anyOf": [
        {
          "$ref": "#/definitions/Lxc"
        },
        {
          "type": "null"
        }
      ],
      "description": "LXC container settings (Linux). Used when containment is `lxc`."
    },
    "network": {
      "anyOf": [
        {
          "$ref": "#/definitions/Network"
        },
        {
          "type": "null"
        }
      ],
      "description": "Network access policy. Shared across all backends."
    },
    "phase": {
      "anyOf": [
        {
          "$ref": "#/definitions/Phase"
        },
        {
          "type": "null"
        }
      ],
      "description": "State-aware lifecycle phase. When present, the request is a state-aware request (`sandboxId` is required for non-provision phases); when absent, the request is one-shot."
    },
    "process": {
      "anyOf": [
        {
          "$ref": "#/definitions/Process"
        },
        {
          "type": "null"
        }
      ],
      "description": "Process to execute and its environment."
    },
    "processContainer": {
      "anyOf": [
        {
          "$ref": "#/definitions/ProcessContainer"
        },
        {
          "type": "null"
        }
      ],
      "description": "ProcessContainer-specific settings (Windows). Used when containment is `processcontainer`."
    },
    "sandboxId": {
      "description": "Sandbox identifier returned by a prior provision request. Required for non-provision state-aware phases.",
      "type": [
        "string",
        "null"
      ]
    },
    "seatbelt": {
      "anyOf": [
        {
          "$ref": "#/definitions/Seatbelt"
        },
        {
          "type": "null"
        }
      ],
      "description": "macOS Seatbelt backend configuration. Used when containment is `seatbelt`."
    },
    "ui": {
      "anyOf": [
        {
          "$ref": "#/definitions/Ui"
        },
        {
          "type": "null"
        }
      ],
      "description": "Cross-platform UI isolation policy."
    },
    "version": {
      "description": "MXC config schema version (semver), e.g. `\"0.8.0-alpha\"`.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object"
}
