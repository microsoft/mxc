{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/microsoft/mxc/schemas/dev/mxc-config.schema.0.9.0-alpha.json",
  "title": "MXC Configuration 0.9.0-alpha",
  "description": "Exact mutable MXC development configuration contract.",
  "$comment": "GENERATED FILE - DO NOT EDIT. Regenerate with: cargo run --manifest-path src/Cargo.toml -p mxc_schema_gen -- schema --version 0.9.0-alpha --out schemas/dev/mxc-config.schema.0.9.0-alpha.json. This exact contract is not enforced by the runtime parser until Phase 9. Request roots are selected by phase and provision containment.",
  "allOf": [
    {
      "else": {
        "$ref": "#/definitions/OneShotRequest"
      },
      "if": {
        "required": [
          "phase"
        ]
      },
      "then": {
        "else": {
          "else": {
            "else": {
              "else": {
                "else": false,
                "if": {
                  "properties": {
                    "phase": {
                      "const": "deprovision"
                    }
                  },
                  "required": [
                    "phase"
                  ]
                },
                "then": {
                  "$ref": "#/definitions/DeprovisionRequest"
                }
              },
              "if": {
                "properties": {
                  "phase": {
                    "const": "stop"
                  }
                },
                "required": [
                  "phase"
                ]
              },
              "then": {
                "$ref": "#/definitions/StopRequest"
              }
            },
            "if": {
              "properties": {
                "phase": {
                  "const": "exec"
                }
              },
              "required": [
                "phase"
              ]
            },
            "then": {
              "$ref": "#/definitions/ExecRequest"
            }
          },
          "if": {
            "properties": {
              "phase": {
                "const": "start"
              }
            },
            "required": [
              "phase"
            ]
          },
          "then": {
            "$ref": "#/definitions/StartRequest"
          }
        },
        "if": {
          "properties": {
            "phase": {
              "const": "provision"
            }
          },
          "required": [
            "phase"
          ]
        },
        "then": {
          "else": {
            "else": {
              "else": false,
              "if": {
                "properties": {
                  "containment": {
                    "const": "wslc"
                  }
                },
                "required": [
                  "containment"
                ]
              },
              "then": {
                "$ref": "#/definitions/WslcProvisionRequest"
              }
            },
            "if": {
              "properties": {
                "containment": {
                  "const": "isolation_session"
                }
              },
              "required": [
                "containment"
              ]
            },
            "then": {
              "$ref": "#/definitions/IsolationSessionProvisionRequest"
            }
          },
          "if": {
            "properties": {
              "containment": {
                "const": "windows_sandbox"
              }
            },
            "required": [
              "containment"
            ]
          },
          "then": {
            "$ref": "#/definitions/WindowsSandboxProvisionRequest"
          }
        }
      }
    }
  ],
  "definitions": {
    "CaptureDenials": {
      "additionalProperties": false,
      "description": "Windows denial-capture settings.",
      "properties": {
        "mode": {
          "$ref": "#/definitions/CaptureDenialsMode",
          "description": "How each ungranted access is handled while it is recorded."
        },
        "outputPath": {
          "description": "Optional destination for the generated denial report.",
          "type": "string"
        },
        "retainEtl": {
          "description": "Whether to retain the sealed ETL trace after analysis. Retained traces can contain sensitive resource paths and identifiers; callers are responsible for deleting them.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CaptureDenialsMode": {
      "oneOf": [
        {
          "description": "Block",
          "enum": [
            "block"
          ],
          "type": "string"
        },
        {
          "description": "Allow",
          "enum": [
            "allow"
          ],
          "type": "string"
        }
      ]
    },
    "DefaultNetworkPolicy": {
      "oneOf": [
        {
          "description": "Allow",
          "enum": [
            "allow"
          ],
          "type": "string"
        },
        {
          "description": "Block",
          "enum": [
            "block"
          ],
          "type": "string"
        }
      ]
    },
    "DeprovisionExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by the `deprovision` phase.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "DeprovisionPhase": {
      "enum": [
        "deprovision"
      ],
      "type": "string"
    },
    "DeprovisionRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `deprovision` request.",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "correlationVector": {
          "description": "Optional correlation vector relayed from provision.",
          "type": "string"
        },
        "experimental": {
          "$ref": "#/definitions/DeprovisionExperimental",
          "description": "Optional closed post-provision experimental settings."
        },
        "phase": {
          "$ref": "#/definitions/DeprovisionPhase",
          "description": "Exact `deprovision` phase marker."
        },
        "sandboxId": {
          "description": "Identifier of the sandbox to deprovision.",
          "type": "string"
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "phase",
        "sandboxId",
        "version"
      ],
      "type": "object"
    },
    "ExecExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by the `exec` phase.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "ExecPhase": {
      "enum": [
        "exec"
      ],
      "type": "string"
    },
    "ExecRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `exec` request.",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "correlationVector": {
          "description": "Optional correlation vector relayed from provision.",
          "type": "string"
        },
        "experimental": {
          "$ref": "#/definitions/ExecExperimental",
          "description": "Optional closed exec experimental settings."
        },
        "network": {
          "$ref": "#/definitions/Network",
          "description": "Optional per-execution network settings."
        },
        "phase": {
          "$ref": "#/definitions/ExecPhase",
          "description": "Exact `exec` phase marker."
        },
        "process": {
          "$ref": "#/definitions/Process",
          "description": "Process to execute in the sandbox."
        },
        "sandboxId": {
          "description": "Identifier of the sandbox to execute in.",
          "type": "string"
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "phase",
        "process",
        "sandboxId",
        "version"
      ],
      "type": "object"
    },
    "Fallback": {
      "additionalProperties": false,
      "description": "Operator consent for containment fallback behavior.",
      "properties": {
        "allowDaclMutation": {
          "description": "Whether the runtime may mutate host filesystem DACLs as a fallback.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Filesystem": {
      "additionalProperties": false,
      "description": "Filesystem access policy.",
      "properties": {
        "deniedPaths": {
          "description": "Optional paths denied access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "readonlyPaths": {
          "description": "Optional paths granted read-only access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "readwritePaths": {
          "description": "Optional paths granted read-write access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "IsolationSessionContainment": {
      "enum": [
        "isolation_session"
      ],
      "type": "string"
    },
    "IsolationSessionNetwork": {
      "additionalProperties": false,
      "description": "The exact unrestricted-network acknowledgment required when provisioning an IsolationSession.",
      "properties": {
        "allowLocalNetwork": {
          "$ref": "#/definitions/True",
          "description": "Required acknowledgment that local network access is allowed."
        },
        "defaultPolicy": {
          "$ref": "#/definitions/IsolationSessionNetworkDefaultPolicy",
          "description": "Exact `allow` default network policy marker."
        }
      },
      "required": [
        "allowLocalNetwork",
        "defaultPolicy"
      ],
      "type": "object"
    },
    "IsolationSessionNetworkDefaultPolicy": {
      "enum": [
        "allow"
      ],
      "type": "string"
    },
    "IsolationSessionProvision": {
      "additionalProperties": false,
      "description": "IsolationSession settings accepted during provisioning.",
      "properties": {
        "appId": {
          "description": "Optional application identifier carried by the sandbox identity.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "IsolationSessionProvisionExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by an IsolationSession provision request.",
      "properties": {
        "isolation_session": {
          "$ref": "#/definitions/StateAwareIsolationSession",
          "description": "Optional IsolationSession backend settings."
        },
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "IsolationSessionProvisionRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `provision` request for isolation_session",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "containment": {
          "$ref": "#/definitions/IsolationSessionContainment",
          "description": "Exact `isolation_session` containment marker."
        },
        "experimental": {
          "$ref": "#/definitions/IsolationSessionProvisionExperimental",
          "description": "Optional closed experimental settings."
        },
        "network": {
          "$ref": "#/definitions/IsolationSessionNetwork",
          "description": "Required unrestricted-network acknowledgment."
        },
        "phase": {
          "$ref": "#/definitions/ProvisionPhase",
          "description": "Exact `provision` phase marker."
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "containment",
        "network",
        "phase",
        "version"
      ],
      "type": "object"
    },
    "LaunchMethod": {
      "oneOf": [
        {
          "description": "Exec",
          "enum": [
            "exec"
          ],
          "type": "string"
        },
        {
          "description": "Open",
          "enum": [
            "open"
          ],
          "type": "string"
        }
      ]
    },
    "Lifecycle": {
      "additionalProperties": false,
      "description": "Container lifecycle settings.",
      "properties": {
        "destroyOnExit": {
          "description": "Whether to destroy the container when execution ends.",
          "type": "boolean"
        },
        "preservePolicy": {
          "description": "Whether to preserve applied policy after execution ends.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Lxc": {
      "additionalProperties": false,
      "description": "Linux LXC distribution settings.",
      "properties": {
        "distribution": {
          "description": "The Linux distribution name.",
          "type": "string"
        },
        "release": {
          "description": "The distribution release.",
          "type": "string"
        }
      },
      "required": [
        "distribution",
        "release"
      ],
      "type": "object"
    },
    "Network": {
      "additionalProperties": false,
      "description": "Network access policy shared by the stable containment backends.",
      "properties": {
        "allowLocalNetwork": {
          "description": "Optional permission to bind and accept local network connections.",
          "type": "boolean"
        },
        "allowedHosts": {
          "description": "Optional hosts allowed when the default policy blocks access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockedHosts": {
          "description": "Optional hosts blocked when the default policy allows access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "defaultPolicy": {
          "$ref": "#/definitions/DefaultNetworkPolicy",
          "description": "Optional default network posture."
        },
        "egress": {
          "$ref": "#/definitions/NetworkEgress",
          "description": "Optional outbound network rules."
        },
        "enforcementMode": {
          "$ref": "#/definitions/NetworkEnforcementMode",
          "description": "Optional network enforcement mechanism."
        },
        "ingress": {
          "$ref": "#/definitions/NetworkIngress",
          "description": "Optional inbound and host-loopback network rules."
        },
        "proxy": {
          "$ref": "#/definitions/NetworkProxy",
          "description": "Optional proxy configuration."
        }
      },
      "type": "object"
    },
    "NetworkAction": {
      "oneOf": [
        {
          "description": "Allow",
          "enum": [
            "allow"
          ],
          "type": "string"
        },
        {
          "description": "Deny",
          "enum": [
            "deny"
          ],
          "type": "string"
        }
      ]
    },
    "NetworkEgress": {
      "additionalProperties": false,
      "description": "Outbound network policy.",
      "properties": {
        "allow": {
          "description": "Optional explicit allow rules.",
          "items": {
            "$ref": "#/definitions/NetworkRule"
          },
          "type": "array"
        },
        "default": {
          "$ref": "#/definitions/NetworkAction",
          "description": "Optional action applied when no explicit rule matches."
        },
        "deny": {
          "description": "Optional explicit deny rules. Deny takes precedence over allow.",
          "items": {
            "$ref": "#/definitions/NetworkRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "NetworkEnforcementMode": {
      "oneOf": [
        {
          "description": "Capabilities",
          "enum": [
            "capabilities"
          ],
          "type": "string"
        },
        {
          "description": "Firewall",
          "enum": [
            "firewall"
          ],
          "type": "string"
        },
        {
          "description": "Both",
          "enum": [
            "both"
          ],
          "type": "string"
        }
      ]
    },
    "NetworkIngress": {
      "additionalProperties": false,
      "description": "Inbound and host-loopback network policy.",
      "properties": {
        "default": {
          "$ref": "#/definitions/NetworkAction",
          "description": "Optional default action for private-network inbound traffic."
        },
        "hostLoopback": {
          "$ref": "#/definitions/NetworkAction",
          "description": "Optional bidirectional host-loopback connectivity action."
        }
      },
      "type": "object"
    },
    "NetworkPeer": {
      "additionalProperties": false,
      "description": "A CIDR destination, optionally excluding narrower ranges within it.",
      "properties": {
        "cidr": {
          "description": "The IPv4 or IPv6 CIDR this destination matches.",
          "type": "string"
        },
        "except": {
          "description": "Optional CIDRs excluded from this destination.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "cidr"
      ],
      "type": "object"
    },
    "NetworkPort": {
      "additionalProperties": false,
      "description": "A protocol and destination-port selector.",
      "properties": {
        "endPort": {
          "description": "Optional inclusive end of a destination-port range. Requires `port`.",
          "maximum": 65535,
          "minimum": 1.0,
          "type": "integer"
        },
        "port": {
          "description": "Optional destination port. Omission matches every port.",
          "maximum": 65535,
          "minimum": 1.0,
          "type": "integer"
        },
        "protocol": {
          "$ref": "#/definitions/NetworkProtocol",
          "description": "Optional transport protocol. Defaults to `any`."
        }
      },
      "type": "object"
    },
    "NetworkProtocol": {
      "oneOf": [
        {
          "description": "Tcp",
          "enum": [
            "tcp"
          ],
          "type": "string"
        },
        {
          "description": "Udp",
          "enum": [
            "udp"
          ],
          "type": "string"
        },
        {
          "description": "Icmp",
          "enum": [
            "icmp"
          ],
          "type": "string"
        },
        {
          "description": "Any",
          "enum": [
            "any"
          ],
          "type": "string"
        }
      ]
    },
    "NetworkProxy": {
      "description": "One of the proxy configurations accepted by the `0.9.0-alpha` contract.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Connect to an existing proxy on a non-zero localhost TCP port.",
          "properties": {
            "localhost": {
              "maximum": 65535,
              "minimum": 1.0,
              "type": "integer"
            }
          },
          "required": [
            "localhost"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Start and use MXC's built-in test proxy.",
          "properties": {
            "builtinTestServer": {
              "$ref": "#/definitions/True"
            }
          },
          "required": [
            "builtinTestServer"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Connect through the supplied proxy URL.",
          "properties": {
            "url": {
              "type": "string"
            }
          },
          "required": [
            "url"
          ],
          "type": "object"
        }
      ]
    },
    "NetworkRule": {
      "additionalProperties": false,
      "description": "One outbound rule, matching destinations and ports.",
      "properties": {
        "ports": {
          "description": "Optional destination protocols and ports. Omission matches all.",
          "items": {
            "$ref": "#/definitions/NetworkPort"
          },
          "minItems": 1,
          "type": "array"
        },
        "to": {
          "description": "Optional destination CIDRs. Omission matches both IP families.",
          "items": {
            "$ref": "#/definitions/NetworkPeer"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "type": "object"
    },
    "NonEmptyString": {
      "minLength": 1,
      "type": "string"
    },
    "OneShotContainment": {
      "oneOf": [
        {
          "description": "Process",
          "enum": [
            "process"
          ],
          "type": "string"
        },
        {
          "description": "ProcessContainer",
          "enum": [
            "processcontainer"
          ],
          "type": "string"
        },
        {
          "description": "ProcessContainer",
          "enum": [
            "appcontainer"
          ],
          "type": "string"
        },
        {
          "description": "Lxc",
          "enum": [
            "lxc"
          ],
          "type": "string"
        },
        {
          "description": "Bubblewrap",
          "enum": [
            "bubblewrap"
          ],
          "type": "string"
        },
        {
          "description": "Seatbelt",
          "enum": [
            "seatbelt"
          ],
          "type": "string"
        },
        {
          "description": "Seatbelt",
          "enum": [
            "macos_sandbox"
          ],
          "type": "string"
        },
        {
          "description": "Vm",
          "enum": [
            "vm"
          ],
          "type": "string"
        },
        {
          "description": "WindowsSandbox",
          "enum": [
            "windows_sandbox"
          ],
          "type": "string"
        },
        {
          "description": "Microvm",
          "enum": [
            "microvm"
          ],
          "type": "string"
        },
        {
          "description": "Hyperlight",
          "enum": [
            "hyperlight"
          ],
          "type": "string"
        },
        {
          "description": "Wslc",
          "enum": [
            "wslc"
          ],
          "type": "string"
        },
        {
          "description": "IsolationSession",
          "enum": [
            "isolation_session"
          ],
          "type": "string"
        }
      ]
    },
    "OneShotExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        },
        "test": {
          "$ref": "#/definitions/TestFeature",
          "description": "Optional placeholder test feature."
        },
        "windows_sandbox": {
          "$ref": "#/definitions/OneShotWindowsSandbox",
          "description": "Optional one-shot Windows Sandbox compatibility settings."
        },
        "wslc": {
          "$ref": "#/definitions/OneShotWslc",
          "description": "Optional one-shot WSLC backend settings."
        }
      },
      "type": "object"
    },
    "OneShotRequest": {
      "additionalProperties": false,
      "allOf": [
        {
          "not": {
            "required": [
              "processContainer",
              "appContainer"
            ]
          }
        },
        {
          "not": {
            "required": [
              "seatbelt",
              "macos_sandbox"
            ]
          }
        }
      ],
      "description": "A complete one-shot `0.9.0-alpha` configuration request.",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "appContainer": {
          "$ref": "#/definitions/ProcessContainer",
          "description": "Optional ProcessContainer settings. The legacy `appContainer` spelling is accepted as an alias."
        },
        "containerId": {
          "description": "Optional externally assigned container identifier.",
          "type": "string"
        },
        "containment": {
          "$ref": "#/definitions/OneShotContainment",
          "description": "Optional containment selection."
        },
        "experimental": {
          "$ref": "#/definitions/OneShotExperimental",
          "description": "Optional experimental settings."
        },
        "fallback": {
          "$ref": "#/definitions/Fallback",
          "description": "Optional fallback consent."
        },
        "filesystem": {
          "$ref": "#/definitions/Filesystem",
          "description": "Optional filesystem policy."
        },
        "lifecycle": {
          "$ref": "#/definitions/Lifecycle",
          "description": "Optional lifecycle settings."
        },
        "lxc": {
          "$ref": "#/definitions/Lxc",
          "description": "Optional LXC distribution settings."
        },
        "macos_sandbox": {
          "$ref": "#/definitions/Seatbelt",
          "description": "Optional macOS Seatbelt configuration."
        },
        "network": {
          "$ref": "#/definitions/Network",
          "description": "Optional network policy."
        },
        "process": {
          "$ref": "#/definitions/Process",
          "description": "The process to execute."
        },
        "processContainer": {
          "$ref": "#/definitions/ProcessContainer",
          "description": "Optional ProcessContainer settings. The legacy `appContainer` spelling is accepted as an alias."
        },
        "runtimeConfig": {
          "$ref": "#/definitions/RuntimeConfig",
          "description": "Optional runtime configuration settings."
        },
        "seatbelt": {
          "$ref": "#/definitions/Seatbelt",
          "description": "Optional macOS Seatbelt configuration."
        },
        "ui": {
          "$ref": "#/definitions/Ui",
          "description": "Optional cross-platform user-interface policy."
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "The exact contract version marker."
        }
      },
      "required": [
        "process",
        "version"
      ],
      "type": "object"
    },
    "OneShotWindowsSandbox": {
      "additionalProperties": false,
      "description": "Compatibility settings accepted for one-shot Windows Sandbox requests.",
      "properties": {
        "daemonPipeName": {
          "description": "Optional daemon named-pipe override.",
          "type": "string"
        },
        "idleTimeout": {
          "description": "Legacy idle-timeout field retained for compatibility.",
          "maximum": 4294967295,
          "minimum": 0.0,
          "type": "integer"
        },
        "idleTimeoutMs": {
          "description": "Idle timeout before teardown, in milliseconds.",
          "maximum": 4294967295,
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "OneShotWslc": {
      "additionalProperties": false,
      "description": "One-shot WSLC backend settings.",
      "properties": {
        "cpuCount": {
          "description": "Requested virtual CPU count.",
          "maximum": 4294967295,
          "minimum": 0.0,
          "type": "integer"
        },
        "gpu": {
          "description": "Whether GPU passthrough is enabled.",
          "type": "boolean"
        },
        "image": {
          "description": "Container image reference.",
          "type": "string"
        },
        "imageTarPath": {
          "description": "Path to a local image tarball to import.",
          "type": "string"
        },
        "memoryMb": {
          "description": "Requested memory limit in megabytes.",
          "maximum": 18446744073709551615,
          "minimum": 0.0,
          "type": "integer"
        },
        "portMappings": {
          "description": "Optional host-to-container TCP port mappings.",
          "items": {
            "$ref": "#/definitions/PortMapping"
          },
          "type": "array"
        },
        "storagePath": {
          "description": "Optional storage path override.",
          "type": "string"
        },
        "targetOs": {
          "description": "Target operating system inside the container.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PortMapping": {
      "additionalProperties": false,
      "description": "A host-to-container WSLC port mapping.",
      "properties": {
        "containerPort": {
          "description": "Non-zero TCP port inside the container.",
          "maximum": 65535,
          "minimum": 1.0,
          "type": "integer"
        },
        "protocol": {
          "$ref": "#/definitions/TransportProtocol",
          "description": "Optional transport protocol. Only TCP is currently supported."
        },
        "windowsPort": {
          "description": "Non-zero TCP port on the Windows host.",
          "maximum": 65535,
          "minimum": 1.0,
          "type": "integer"
        }
      },
      "required": [
        "containerPort",
        "windowsPort"
      ],
      "type": "object"
    },
    "Process": {
      "additionalProperties": false,
      "description": "Process execution settings.",
      "properties": {
        "commandLine": {
          "$ref": "#/definitions/NonEmptyString",
          "description": "The non-empty command line to execute."
        },
        "cwd": {
          "description": "Optional working directory.",
          "type": "string"
        },
        "env": {
          "description": "Optional environment entries encoded as `KEY=VALUE` strings.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "parentTaskId": {
          "description": "Optional existing IsoTask parent for the launched process.",
          "type": "string"
        },
        "taskDisplayName": {
          "description": "Optional friendly IsoTask display name for the launched process.",
          "type": "string"
        },
        "timeout": {
          "description": "Optional execution timeout in milliseconds.",
          "maximum": 4294967295,
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "commandLine"
      ],
      "type": "object"
    },
    "ProcessContainer": {
      "additionalProperties": false,
      "description": "ProcessContainer-specific settings.",
      "properties": {
        "capabilities": {
          "description": "Optional AppContainer capability names. Each entry must contain one name; commas are rejected because BaseContainer uses them as its wire delimiter. `learningModeLogging` and `permissiveLearningMode` are reserved; use `learningMode`, `--audit`, or `captureDenials` instead.",
          "items": {
            "$ref": "#/definitions/ProcessContainerCapability"
          },
          "type": "array"
        },
        "captureDenials": {
          "$ref": "#/definitions/CaptureDenials",
          "description": "Optional capture-denials policy."
        },
        "learningMode": {
          "description": "Optional learning-mode (deny-and-record)",
          "type": "boolean"
        },
        "leastPrivilege": {
          "description": "Whether least-privilege mode is enabled.",
          "type": "boolean"
        },
        "network": {
          "$ref": "#/definitions/ProcessContainerNetwork",
          "description": "Optional ProcessContainer-specific network settings."
        },
        "ui": {
          "$ref": "#/definitions/ProcessContainerUi",
          "description": "Optional ProcessContainer-specific user-interface policy."
        }
      },
      "type": "object"
    },
    "ProcessContainerCapability": {
      "pattern": "^(?![\\s\\S]*,)(?!(?:[lL][eE][aA][rR][nN][iI][nN][gG][mM][oO][dD][eE][lL][oO][gG][gG][iI][nN][gG]|[pP][eE][rR][mM][iI][sS][sS][iI][vV][eE][lL][eE][aA][rR][nN][iI][nN][gG][mM][oO][dD][eE])$)[\\s\\S]*$",
      "type": "string"
    },
    "ProcessContainerNetwork": {
      "additionalProperties": false,
      "description": "ProcessContainer-specific network settings.",
      "properties": {
        "allowedProxyPeer": {
          "description": "Optional loopback peer the contained process may reach in addition to the configured runtime proxy. Requires `runtimeConfig.networkProxy`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ProcessContainerUi": {
      "additionalProperties": false,
      "description": "ProcessContainer-specific user-interface policy.",
      "properties": {
        "desktopSystemControl": {
          "description": "Whether desktop system control is allowed.",
          "type": "boolean"
        },
        "ime": {
          "description": "Whether Input Method Editor access is allowed.",
          "type": "boolean"
        },
        "isolation": {
          "$ref": "#/definitions/ProcessContainerUiIsolation",
          "description": "Optional desktop-resource isolation level."
        },
        "systemSettings": {
          "description": "Optional system-settings access level.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ProcessContainerUiIsolation": {
      "oneOf": [
        {
          "description": "Container",
          "enum": [
            "container"
          ],
          "type": "string"
        },
        {
          "description": "Desktop",
          "enum": [
            "desktop"
          ],
          "type": "string"
        },
        {
          "description": "Handles",
          "enum": [
            "handles"
          ],
          "type": "string"
        },
        {
          "description": "Atoms",
          "enum": [
            "atoms"
          ],
          "type": "string"
        }
      ]
    },
    "ProvisionPhase": {
      "enum": [
        "provision"
      ],
      "type": "string"
    },
    "RuntimeConfig": {
      "additionalProperties": false,
      "description": "Runtime configuration supplied alongside the sandbox policy.",
      "properties": {
        "networkProxy": {
          "description": "Optional loopback proxy the runtime configures for the sandbox. Must address localhost, and requires an egress policy.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Seatbelt": {
      "additionalProperties": false,
      "description": "macOS Seatbelt backend settings.",
      "properties": {
        "extraMachLookups": {
          "description": "Additional Mach service global names the process may resolve.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "guiAccess": {
          "description": "Whether GUI application access is allowed.",
          "type": "boolean"
        },
        "keychainAccess": {
          "description": "Whether macOS Keychain access is allowed.",
          "type": "boolean"
        },
        "launchMethod": {
          "$ref": "#/definitions/LaunchMethod",
          "description": "Optional method used to launch the contained process."
        },
        "nestedPty": {
          "description": "Whether the contained process may allocate nested pseudo-terminals.",
          "type": "boolean"
        },
        "profileOverride": {
          "description": "Optional override of the generated sandbox profile.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "StartExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by the `start` phase.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "StartPhase": {
      "enum": [
        "start"
      ],
      "type": "string"
    },
    "StartRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `start` request.",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "correlationVector": {
          "description": "Optional correlation vector relayed from provision.",
          "type": "string"
        },
        "experimental": {
          "$ref": "#/definitions/StartExperimental",
          "description": "Optional closed post-provision experimental settings."
        },
        "phase": {
          "$ref": "#/definitions/StartPhase",
          "description": "Exact `start` phase marker."
        },
        "sandboxId": {
          "description": "Identifier returned by the provision phase.",
          "type": "string"
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "phase",
        "sandboxId",
        "version"
      ],
      "type": "object"
    },
    "StateAwareIsolationSession": {
      "additionalProperties": false,
      "description": "State-aware IsolationSession experimental settings.",
      "properties": {
        "provision": {
          "$ref": "#/definitions/IsolationSessionProvision",
          "description": "Optional provision-phase settings."
        }
      },
      "type": "object"
    },
    "StateAwareWslc": {
      "additionalProperties": false,
      "description": "State-aware WSLC experimental settings.",
      "properties": {
        "provision": {
          "$ref": "#/definitions/WslcProvision",
          "description": "Optional provision-phase settings."
        }
      },
      "type": "object"
    },
    "StopExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by the `stop` phase.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "StopPhase": {
      "enum": [
        "stop"
      ],
      "type": "string"
    },
    "StopRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `stop` request.",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "correlationVector": {
          "description": "Optional correlation vector relayed from provision.",
          "type": "string"
        },
        "experimental": {
          "$ref": "#/definitions/StopExperimental",
          "description": "Optional closed post-provision experimental settings."
        },
        "phase": {
          "$ref": "#/definitions/StopPhase",
          "description": "Exact `stop` phase marker."
        },
        "sandboxId": {
          "description": "Identifier returned by the provision phase.",
          "type": "string"
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "phase",
        "sandboxId",
        "version"
      ],
      "type": "object"
    },
    "Telemetry": {
      "additionalProperties": false,
      "description": "One-shot telemetry override.",
      "properties": {
        "enabled": {
          "description": "Whether telemetry is enabled.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TestFeature": {
      "additionalProperties": false,
      "description": "Placeholder feature used to exercise experimental configuration plumbing.",
      "properties": {
        "message": {
          "description": "The message for the test feature.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TransportProtocol": {
      "oneOf": [
        {
          "description": "Tcp",
          "enum": [
            "tcp"
          ],
          "type": "string"
        }
      ]
    },
    "True": {
      "enum": [
        true
      ],
      "type": "boolean"
    },
    "Ui": {
      "additionalProperties": false,
      "description": "Cross-platform user-interface policy.",
      "properties": {
        "clipboard": {
          "$ref": "#/definitions/UiClipboard",
          "description": "Optional clipboard access level."
        },
        "disable": {
          "description": "Whether visible user interface is disabled.",
          "type": "boolean"
        },
        "injection": {
          "description": "Whether keyboard and mouse input injection is allowed.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "UiClipboard": {
      "oneOf": [
        {
          "description": "None",
          "enum": [
            "none"
          ],
          "type": "string"
        },
        {
          "description": "Read",
          "enum": [
            "read"
          ],
          "type": "string"
        },
        {
          "description": "Write",
          "enum": [
            "write"
          ],
          "type": "string"
        },
        {
          "description": "All",
          "enum": [
            "all"
          ],
          "type": "string"
        }
      ]
    },
    "Version": {
      "oneOf": [
        {
          "description": "V0_9_0Alpha",
          "enum": [
            "0.9.0-alpha"
          ],
          "type": "string"
        }
      ]
    },
    "WindowsSandboxContainment": {
      "enum": [
        "windows_sandbox"
      ],
      "type": "string"
    },
    "WindowsSandboxExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by a Windows Sandbox provision request.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        }
      },
      "type": "object"
    },
    "WindowsSandboxProvisionRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `provision` request for windows_sandbox",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "containment": {
          "$ref": "#/definitions/WindowsSandboxContainment",
          "description": "Exact `windows_sandbox` containment marker."
        },
        "experimental": {
          "$ref": "#/definitions/WindowsSandboxExperimental",
          "description": "Optional closed experimental settings."
        },
        "filesystem": {
          "$ref": "#/definitions/Filesystem",
          "description": "Optional filesystem policy."
        },
        "phase": {
          "$ref": "#/definitions/ProvisionPhase",
          "description": "Exact `provision` phase marker."
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "containment",
        "phase",
        "version"
      ],
      "type": "object"
    },
    "WslcContainment": {
      "enum": [
        "wslc"
      ],
      "type": "string"
    },
    "WslcProvision": {
      "additionalProperties": false,
      "description": "WSLC settings accepted during provisioning.",
      "properties": {
        "image": {
          "description": "Optional container image reference.",
          "type": "string"
        },
        "imageTarPath": {
          "description": "Optional path to a local container image archive.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "WslcProvisionExperimental": {
      "additionalProperties": false,
      "description": "Experimental settings accepted by a WSLC provision request.",
      "properties": {
        "telemetry": {
          "$ref": "#/definitions/Telemetry",
          "description": "Optional telemetry override."
        },
        "wslc": {
          "$ref": "#/definitions/StateAwareWslc",
          "description": "Optional WSLC backend settings."
        }
      },
      "type": "object"
    },
    "WslcProvisionRequest": {
      "additionalProperties": false,
      "description": "A complete state-aware `provision` request for wslc",
      "properties": {
        "$schema": {
          "description": "Optional JSON Schema reference for editor validation.",
          "type": "string"
        },
        "_comment": {
          "description": "Optional human-readable annotation ignored by the runtime."
        },
        "containment": {
          "$ref": "#/definitions/WslcContainment",
          "description": "Exact `wslc` containment marker."
        },
        "experimental": {
          "$ref": "#/definitions/WslcProvisionExperimental",
          "description": "Optional closed experimental settings."
        },
        "filesystem": {
          "$ref": "#/definitions/Filesystem",
          "description": "Optional filesystem policy fixed at provision time."
        },
        "network": {
          "$ref": "#/definitions/Network",
          "description": "Optional network policy fixed at provision time."
        },
        "phase": {
          "$ref": "#/definitions/ProvisionPhase",
          "description": "Exact `provision` phase marker."
        },
        "version": {
          "$ref": "#/definitions/Version",
          "description": "Exact development contract version."
        }
      },
      "required": [
        "containment",
        "phase",
        "version"
      ],
      "type": "object"
    }
  }
}
