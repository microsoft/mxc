{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/microsoft/mxc/schemas/mxc-config.v1.schema.json",
  "title": "MXC Configuration",
  "description": "Configuration schema for MXC container execution engine (v1)",
  "type": "object",

  "anyOf": [
    { "required": ["script"] },
    {
      "required": ["process"],
      "properties": {
        "process": {
          "anyOf": [
            { "required": ["commandLine"] }
          ]
        }
      }
    }
  ],

  "properties": {
    "version": {
      "type": "string",
      "description": "MXC config schema version. Current: '1'.",
      "examples": ["1"]
    },
    "containerId": {
      "type": "string",
      "description": "Externally assigned container identifier."
    },
    "containment": {
      "type": "string",
      "enum": ["appcontainer", "sandbox", "vm", "wslc", "lxc", "microvm"],
      "default": "appcontainer",
      "description": "Containment backend to use for execution."
    },
    "platform": {
      "type": "string",
      "enum": ["linux", "windows"],
      "default": "windows",
      "description": "Target platform. Must be compatible with the selected containment backend."
    },

    "lifecycle": {
      "type": "object",
      "description": "Container lifecycle settings shared across all backends.",
      "properties": {
        "destroyOnExit": {
          "type": "boolean",
          "default": true,
          "description": "Destroy the container after execution completes."
        },
        "preservePolicy": {
          "type": "boolean",
          "default": false,
          "description": "If true, retain filesystem and network policies after execution. If false (default), policies are cleaned up on exit."
        }
      },
      "additionalProperties": false
    },

    "process": {
      "type": "object",
      "description": "Process execution settings.",
      "properties": {
        "commandLine": {
          "type": "string",
          "minLength": 1,
          "description": "Complete command line to execute (e.g., \"python3 app.py\")."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory for the process."
        },
        "env": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Environment variables as KEY=VALUE strings.",
          "examples": [["MY_VAR=value", "PATH=/usr/bin"]]
        },
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Execution timeout in milliseconds. 0 = no timeout (infinite)."
        }
      },
      "additionalProperties": false
    },

    "filesystem": {
      "type": "object",
      "description": "Filesystem access policy. Shared across all backends.",
      "properties": {
        "readwritePaths": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Paths the process can read and write."
        },
        "readonlyPaths": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Paths the process can read but not write."
        },
        "deniedPaths": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Paths the process cannot access at all."
        }
      },
      "additionalProperties": false
    },

    "network": {
      "type": "object",
      "description": "Network access policy. Shared across all backends.",
      "properties": {
        "defaultPolicy": {
          "type": "string",
          "enum": ["allow", "block"],
          "default": "allow",
          "description": "Default network posture."
        },
        "allowedHosts": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Hostnames or IP/CIDR blocks to allow (when defaultPolicy is 'block')."
        },
        "blockedHosts": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Hostnames or IP addresses to block (when defaultPolicy is 'allow')."
        },
        "enforcementMode": {
          "type": "string",
          "enum": ["capabilities", "firewall", "both"],
          "default": "capabilities",
          "description": "How network policy is enforced."
        },
        "proxy": {
          "type": "object",
          "description": "Proxy configuration. Only supported with appcontainer backend.",
          "oneOf": [
            {
              "properties": {
                "localhost": {
                  "type": "integer",
                  "minimum": 1,
                  "maximum": 65535,
                  "description": "Port of a localhost proxy."
                }
              },
              "required": ["localhost"],
              "additionalProperties": false
            },
            {
              "properties": {
                "builtinTestServer": {
                  "type": "boolean",
                  "const": true,
                  "description": "Use WXC's built-in test proxy server."
                }
              },
              "required": ["builtinTestServer"],
              "additionalProperties": false
            }
          ]
        }
      },
      "additionalProperties": false
    },

    "appContainer": {
      "type": "object",
      "description": "AppContainer-specific settings. Used when containment is 'appcontainer'.",
      "properties": {
        "leastPrivilege": {
          "type": "boolean",
          "default": false,
          "description": "Enforce least privilege mode."
        },
        "learningMode": {
          "type": "boolean",
          "default": false,
          "description": "Debug only: disables restrictions. Stripped in release builds."
        },
        "capabilities": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "AppContainer capabilities (e.g., 'internetClient', 'registryRead')."
        }
      },
      "additionalProperties": false
    },

    "sandbox": {
      "type": "object",
      "description": "Windows Sandbox-specific settings. Used when containment is 'sandbox'.",
      "properties": {
        "daemonPipeName": {
          "type": "string",
          "default": "wxc-sandbox",
          "description": "Named pipe name for the sandbox daemon."
        },
        "idleTimeoutMs": {
          "type": "integer",
          "minimum": 0,
          "default": 300000,
          "description": "Idle timeout in milliseconds before the daemon tears down the sandbox. 0 = no timeout."
        }
      },
      "additionalProperties": false
    },

    "wslc": {
      "type": "object",
      "description": "WSLC SDK-specific settings. Used when containment is 'wslc'.",
      "properties": {
        "image": {
          "type": "string",
          "default": "alpine:latest",
          "description": "OCI container image name (e.g., 'python:3.12', 'ubuntu:22.04')."
        },
        "storagePath": {
          "type": "string",
          "description": "Storage path for WSLC session image store. Omit for SDK default."
        }
      },
      "additionalProperties": false
    },

    "lxc": {
      "type": "object",
      "description": "LXC-specific settings. Used when containment is 'lxc'.",
      "properties": {
        "distribution": {
          "type": "string",
          "default": "alpine",
          "description": "Linux distribution for the container rootfs."
        },
        "release": {
          "type": "string",
          "default": "3.19",
          "description": "Distribution release version."
        }
      },
      "additionalProperties": false
    }
  },

  "additionalProperties": false
}
