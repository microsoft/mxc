{
  "$comment": "Canonical source of truth for MXC CONFIG SCHEMA versions. Separate from the PRODUCT version (src/Cargo.toml [workspace.package] and sdk/package.json), which tracks the binaries/npm package and is checked independently by check-version-sync.js. Fields: min = lowest schema version the single current parser/SDK accept; maxSupported = highest version they accept (the in-development line); stableLatest = highest released immutable stable schema file; stateAware = schema version state-aware lifecycle requests use; generatedStableFloor = first stable release generated by freeze-stable-schema.js (structural compatibility checks are reliable from this line onward); devSchemaFile = filename suffix of the in-progress dev schema. Because one current wire model parses the whole supported range, a breaking release must advance min to its own major.minor line. Every matching constant in the Rust parser, SDK, and schema filenames must agree with these values; the versioning gates enforce the contract in CI.",
  "min": "0.6.0-alpha",
  "maxSupported": "0.8.0-alpha",
  "stateAware": "0.6.0-alpha",
  "stableLatest": "0.7.0-alpha",
  "generatedStableFloor": "0.8.0-alpha",
  "devSchemaFile": "0.8.0-dev"
}
