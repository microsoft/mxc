[package]
name = "mxc_alpha_learning_mode_windows"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Windows learning-mode backend for MXC: captures AppInfo-brokered ETW denial traces via processmodel.dll and decodes them into cross-platform records."

[dependencies]
thiserror = { workspace = true }

[target.'cfg(target_os = "windows")'.dependencies]
mxc_alpha_wxc_common = { workspace = true }
mxc_alpha_learning_mode_core = { workspace = true }
windows = { workspace = true }
windows-core = { workspace = true }

[target.'cfg(target_os = "windows")'.dev-dependencies]
flatbuffers = { workspace = true }
mxc_alpha_process_security_environment_spec = { workspace = true }
