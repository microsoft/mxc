[package]
name = "mxc-alpha-seatbelt-common"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Seatbelt (macOS) backend support for MXC sandboxing."

[dependencies]
# Provides ExecutionRequest, ContainerPolicy, SeatbeltConfig, Logger, and
# the ScriptRunner trait that seatbelt_runner implements. Same dep that
# lxc_common uses.
mxc-alpha-wxc-common = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
libc = { workspace = true }
