# Generated crate: bindings under src/ are produced by `flatc` from
# external/windows-sdk/ProcessSecurityEnvironment.fbs. Do not hand-edit them —
# see README.md and regenerate.ps1. Provenance (schema hash, pinned flatc
# version, source build) lives in the schema's .provenance.toml.
[package]
name = "process_security_environment_spec"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Generated FlatBuffers bindings for ProcessSecurityEnvironment"

[dependencies]
flatbuffers = { workspace = true }
