[package]
name = "wslc_common"
version = "0.1.0"
edition = "2021"

[features]
default = []
link-wslcsdk = []

[dependencies]
wxc_common = { workspace = true }
windows = { workspace = true }
tar = "0.4"

[dev-dependencies]
tempfile = "3"

[build-dependencies]
zip = "2"
