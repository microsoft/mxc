[package]
name = "wxc_e2e_tests"
version = "0.1.0"
edition.workspace = true
publish = false
description = "End-to-end integration tests for MXC"

# No dependencies needed — tests shell out to PS1 scripts and npm
