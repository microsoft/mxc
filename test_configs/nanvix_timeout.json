{
    "_comment": "Timeout test. Note: NanVix adds a 60s boot grace to the 5s script timeout, so actual wall time is ~65s.",
    "process": {
        "commandLine": "import time; time.sleep(120); print('should not reach here')",
        "timeout": 5000
    },
    "containment": "nanvix"
}
