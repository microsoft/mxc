{
  "version": "0.8.0-alpha",
  "containerId": "CLI-Bubblewrap-Denied-Symlink-Dir",
  "containment": "bubblewrap",
  "process": {
    "commandLine": "B=/mnt/symmask; if cat \"$B/control.txt\" 2>/dev/null | grep -q CONTROL_SECRET; then echo CONTROL_OK; else echo CONTROL_MISSING; fi; if ls \"$B/real_dir\" 2>/dev/null | grep -q .; then echo SYMDIR_LEAK; else echo SYMDIR_MASKED_OK; fi"
  },
  "filesystem": {
    "readwritePaths": ["/mnt/symmask"],
    "deniedPaths": ["/mnt/symmask/link_to_dir"]
  }
}
