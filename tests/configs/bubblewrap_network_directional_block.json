{
  "version": "0.8.0-alpha",
  "containerId": "CLI-Bubblewrap-Network-Directional-Block",
  "containment": "bubblewrap",
  "process": {
    "commandLine": "bash -c 'set -u; echo SANDBOX_NETNS=$(readlink /proc/self/ns/net); timeout 6 bash -c \"exec 3<>/dev/tcp/1.1.1.1/443\" >/dev/null 2>&1; if [ $? = 0 ]; then echo EGRESS_LEAKED; exit 1; fi; echo EGRESS_BLOCKED_OK'"
  },
  "network": {
    "egress": {
      "default": "deny"
    },
    "ingress": {
      "default": "deny",
      "hostLoopback": "deny"
    }
  }
}
