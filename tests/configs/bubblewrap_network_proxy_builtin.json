{
  "version": "0.6.0-alpha",
  "containerId": "CLI-Bubblewrap-Network-Proxy-Builtin",
  "containment": "bubblewrap",
  "process": {
    "commandLine": "curl -fsSL --max-time 15 http://mxc-test.invalid/ | grep -q MXC_TEST_ORIGIN_OK && echo PROXY_OK"
  },
  "network": {
    "defaultPolicy": "allow",
    "proxy": { "builtinTestServer": true }
  }
}
