{
  "version": "0.7.0-alpha",
  "containerId": "CLI-Bubblewrap-Network-Proxy-Legacy",
  "containment": "bubblewrap",
  "process": {
    "commandLine": "set -e; echo SANDBOX_NETNS=$(readlink /proc/self/ns/net); echo SANDBOX_PROXY=$HTTP_PROXY; curl -fsSL https://api.github.com/zen > /dev/null; echo LEGACY_PROXY_OK"
  },
  "network": {
    "defaultPolicy": "allow",
    "proxy": { "builtinTestServer": true }
  }
}
