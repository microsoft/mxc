{
  "version": "0.6.0-alpha",
  "containerId": "CLI-BFSReadOnly-Test",
  "containment": "processcontainer",
  "process": {
    "commandLine": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -NoProfile -NonInteractive -Command \"$path = 'C:\\Windows\\win.ini'; $content = Get-Content -Path $path -Raw -ErrorAction Stop; if ([string]::IsNullOrEmpty($content)) { Write-Output 'FAIL:readonly_read'; exit 1 }; Write-Output 'OK:readonly_read'; try { Set-Content -Path $path -Value 'This should fail' -ErrorAction Stop; Write-Output 'FAIL:readonly_write'; exit 1 } catch { Write-Output 'OK:readonly_write_denied'; exit 0 }\"",
    "timeout": 30000
  },
  "filesystem": {
    "readonlyPaths": [
      "C:\\Windows"
    ]
  },
  "ui": {
    "disable": false
  }
}
