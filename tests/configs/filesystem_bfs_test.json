{
  "version": "0.6.0-alpha",
  "containerId": "CLI-BFS-Test",
  "containment": "processcontainer",
  "process": {
    "commandLine": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -NoProfile -NonInteractive -Command \"$allowed = 'C:\\Users\\Public\\mxc_bfs_allowed'; $outside = 'C:\\ProgramData\\mxc_bfs_outside'; New-Item -ItemType Directory -Path $allowed -Force -ErrorAction Stop | Out-Null; $output = Join-Path $allowed 'test_output.txt'; Set-Content -Path $output -Value 'BFS_ALLOWED' -ErrorAction Stop; if ((Get-Content -Path $output -Raw).Trim() -ne 'BFS_ALLOWED') { Write-Output 'FAIL:allowed_readback'; exit 1 }; Write-Output 'OK:allowed_write'; try { New-Item -ItemType Directory -Path $outside -Force -ErrorAction Stop | Out-Null; Write-Output 'FAIL:outside_create'; exit 1 } catch { Write-Output 'OK:outside_create_denied'; exit 0 }\"",
    "timeout": 30000
  },
  "filesystem": {
    "readwritePaths": [
      "C:\\Users\\Public"
    ],
    "readonlyPaths": [
      "C:\\Windows"
    ]
  },
  "ui": {
    "disable": false
  }
}
