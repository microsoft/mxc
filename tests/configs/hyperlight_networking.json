{
    "version": "0.10.0-alpha",
    "_comment": "Intentional v0.10 migration rejection: the former example.com hostname allowlist has no lossless directional CIDR equivalent. The parser must reject network.allowedHosts before the workload runs; run_hyperlight_network_migration_test.ps1 asserts that boundary without DNS resolution or broader network access.",
    "process": {
        "commandLine": "import urllib.request; r = urllib.request.urlopen('http://example.com/'); print(r.status)",
        "timeout": 30000
    },
    "containment": "hyperlight",
    "network": {
        "allowedHosts": ["example.com"]
    }
}
