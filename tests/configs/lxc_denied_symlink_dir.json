{
  "version": "0.8.0-alpha",
  "containerId": "CLI-LXC-Denied-Symlink-Dir",
  "containment": "lxc",
  "process": {
    "commandLine": "B=/mnt/symmask; if cat \"$B/control.txt\" 2>/dev/null | grep -q CONTROL_SECRET; then echo CONTROL_OK; else echo CONTROL_MISSING; fi; if ls \"$B/link_to_dir\" 2>/dev/null | grep -q .; then echo SYMDIR_LEAK; else echo SYMDIR_MASKED_OK; fi"
  },
  "lifecycle": {
    "destroyOnExit": true
  },
  "lxc": {
    "distribution": "alpine",
    "release": "3.23"
  },
  "filesystem": {
    "readwritePaths": ["/mnt/symmask"],
    "deniedPaths": ["/mnt/symmask/link_to_dir"]
  }
}
