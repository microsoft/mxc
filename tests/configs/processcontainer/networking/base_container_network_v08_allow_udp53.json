{
  "$schema": "../../../../schemas/dev/mxc-config.schema.0.8.0-dev.json",
  "version": "0.8.0-dev",
  "containerId": "CLI-BaseContainer-v08-Allow-UDP53",
  "containment": "processcontainer",
  "process": {
    "commandLine": "powershell.exe -NoProfile -NonInteractive -Command \"$output = (& nslookup.exe example.com 8.8.8.8 2>&1) -join [Environment]::NewLine; if ($output -match 'Addresses?:' -and $output -notmatch 'timed out') { Write-Output 'OK:allow_udp53'; exit 0 }; Write-Output 'FAIL:allow_udp53_blocked'; Write-Output $output; exit 1\"",
    "timeout": 20000
  },
  "filesystem": {
    "readonlyPaths": ["C:\\Windows"]
  },
  "ui": {
    "disable": false
  },
  "network": {
    "egress": {
      "default": "deny",
      "allow": [
        {
          "to": [{"cidr": "8.8.8.8/32"}],
          "ports": [{"protocol": "udp", "port": 53}]
        }
      ]
    }
  }
}
