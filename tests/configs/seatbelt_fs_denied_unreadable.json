{
    "$schema": "../../schemas/stable/mxc-config.schema.0.8.0-alpha.json",
    "version": "0.8.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "cat {{TESTDIR}}/denied/secret.txt && echo FS_DENIED_READ_SUCCEEDED",
        "timeout": 10000
    },
    "filesystem": {
        "readonlyPaths": ["{{TESTDIR}}"],
        "deniedPaths": ["{{TESTDIR}}/denied"]
    }
}
