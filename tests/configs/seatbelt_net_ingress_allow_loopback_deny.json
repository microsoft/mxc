{
    "$schema": "../../schemas/stable/mxc-config.schema.0.8.0-alpha.json",
    "version": "0.8.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "/usr/bin/python3 -c $'import socket as k\ndef t(h,p):\n try:\n  s=k.socket(); s.settimeout(5); s.connect((h,p)); s.close(); return True\n except Exception: return False\ndef b(p):\n try:\n  s=k.socket(); s.setsockopt(k.SOL_SOCKET,k.SO_REUSEADDR,1); s.bind((\"127.0.0.1\",p)); s.listen(1); s.close(); return \"OK\"\n except OSError as e: return \"BLOCKED_%s\" % e.errno\nprint(\"EXTERNAL_OK\" if t(\"1.1.1.1\",443) else \"EXTERNAL_BLOCKED\")\nprint(\"HOSTLOOPBACK_\" + (\"REACHED\" if t(\"127.0.0.1\",{{PORT}}) else \"BLOCKED\"))\nprint(\"LISTEN_\" + b(0))'",
        "timeout": 30000
    },
    "filesystem": { "readonlyPaths": ["{{DEVDIR}}"], "readwritePaths": ["/private/tmp"] },
    "network": {
        "egress": { "default": "deny" },
        "ingress": { "default": "allow", "hostLoopback": "deny" }
    }
}
