{
    "version": "0.8.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "echo SHOULD_NOT_RUN",
        "timeout": 10000
    },
    "network": {
        "egress": { "default": "deny" },
        "ingress": { "default": "allow", "hostLoopback": "deny" }
    }
}
