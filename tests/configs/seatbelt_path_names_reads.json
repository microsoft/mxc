{
    "$schema": "../../schemas/dev/mxc-config.schema.0.10.0-alpha.json",
    "version": "0.10.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "/usr/bin/python3 -c 'def probe(label, path):\n    try:\n        open(path).read()\n        print(label + \"=READ\")\n    except OSError as e:\n        print(label + \"=DENIED(\" + str(e.errno) + \")\")\nroot = \"{{ROOT}}\"\nfor label, rel in [(\"TOP\", \".ssh/id\"), (\"DEEP_CASE\", \"a/b/.SSH/id\"), (\"MULTI\", \"a/.config/gh/hosts.yml\"), (\"SPECIAL_NAME\", \"sub/a+b(1).key\"), (\"LINK_INTO\", \"link-into/id\"), (\"LINK_NAMED\", \"b/.ssh/data.txt\"), (\"SIBLING\", \"a/.config/other.txt\"), (\"BOUNDARY\", \"x.ssh/id\"), (\"DOT_DECOY\", \"xssh/id\"), (\"SPECIAL_DECOY\", \"sub/aab1xkey\"), (\"TARGET\", \"plain/data.txt\")]:\n    probe(label, root + \"/\" + rel)\nprint(\"PATH_NAMES_PROBE_DONE\")'",
        "timeout": 20000
    },
    "filesystem": {
        "readonlyPaths": ["{{DEVDIR}}"],
        "readwritePaths": ["{{ROOT}}", "{{TESTDIR}}/other"]
    },
    "network": {
        "egress": { "default": "allow" },
        "ingress": { "default": "allow", "hostLoopback": "allow" }
    },
    "seatbelt": {
        "deniedPathNames": [".ssh", ".config/gh", "a+b(1).key"],
        "deniedUnixSocketPaths": ["{{ROOT}}"]
    }
}
