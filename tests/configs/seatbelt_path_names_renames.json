{
    "$schema": "../../schemas/dev/mxc-config.schema.0.10.0-alpha.json",
    "version": "0.10.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "/usr/bin/python3 -c 'import os\ndef mv(label, src, dst):\n    try:\n        os.rename(src, dst)\n        print(label + \"=MOVED\")\n    except OSError as e:\n        print(label + \"=REFUSED(\" + str(e.errno) + \")\")\ndef read(label, path):\n    try:\n        open(path).read()\n        print(label + \"=READ\")\n    except OSError as e:\n        print(label + \"=DENIED(\" + str(e.errno) + \")\")\nroot = \"{{ROOT}}\"\nother = \"{{TESTDIR}}/other\"\nmv(\"UNRELATED\", root + \"/renamable\", root + \"/renamed\")\nmv(\"PREFIX\", root + \"/a/.config\", root + \"/a/cfg\")\nread(\"PREFIX_TARGET\", root + \"/a/cfg/gh/hosts.yml\")\nmv(\"MATCH\", root + \"/.ssh\", root + \"/ssh-moved\")\nmv(\"CROSS_ROOT\", root + \"/branch/nested\", other + \"/moved\")\nread(\"CROSS_ROOT_MATCH\", other + \"/moved/.ssh/sentinel\")\nprint(\"PATH_NAMES_PROBE_DONE\")'",
        "timeout": 20000
    },
    "filesystem": {
        "readonlyPaths": ["{{DEVDIR}}"],
        "readwritePaths": ["{{ROOT}}", "{{TESTDIR}}/other"]
    },
    "network": {
        "egress": { "default": "allow" },
        "ingress": { "default": "allow", "hostLoopback": "allow" }
    },
    "seatbelt": {
        "deniedPathNames": [".ssh", ".config/gh", "a+b(1).key"],
        "deniedUnixSocketPaths": ["{{ROOT}}"]
    }
}
