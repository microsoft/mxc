{
  "version": "0.6.0-alpha",
  "containerId": "CLI-WindowsSandbox-Timeout",
  "containment": "windows_sandbox",
  "process": {
    "commandLine": "python -S -B -c \"import sys; print(f'Hello from sandbox Python {sys.version}'); print('Sandbox test successful')\"",
    "timeout": 60000
  }
}
