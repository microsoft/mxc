{
    "$schema": "../../schemas/stable/mxc-config.schema.0.7.0-alpha.json",
    "version": "0.7.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "echo 'Testing network restrictions...' && echo '' && echo 'macOS Seatbelt has no per-host filtering primitive, so this policy is a' && echo 'blanket outbound deny -- allowedHosts is rejected under defaultPolicy=block.' && echo 'For proxy-scoped egress see 30_mac_network_schema_v2.json.' && echo '' && echo 'Test 1: Connecting to api.github.com...' && curl -s --max-time 5 https://api.github.com/zen > /dev/null 2>&1 && echo '  ERROR: Should not have connected!' || echo '  SUCCESS: Connection blocked as expected' && echo '' && echo 'Test 2: Connecting to example.com...' && curl -s --max-time 5 https://example.com > /dev/null 2>&1 && echo '  ERROR: Should not have connected!' || echo '  SUCCESS: Connection blocked as expected' && echo '' && echo 'Network restriction test complete!'",
        "timeout": 30000
    },
    "filesystem": {
        "readwritePaths": [
            "/tmp"
        ]
    },
    "network": {
        "defaultPolicy": "block"
    }
}
