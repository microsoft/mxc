{
    "$schema": "../../schemas/stable/mxc-config.schema.0.7.0-alpha.json",
    "version": "0.7.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "echo 'Combined Filesystem Restrictions + Open Network Test'; echo '============================================================'; echo ''; rc=0; echo 'FILESYSTEM TESTS (restricted):'; echo '------------------------------------------------------------'; echo 'Test 1: Write to allowed path (/tmp/mxc_combined)...'; if mkdir -p /tmp/mxc_combined && echo 'sandbox data' > /tmp/mxc_combined/result.txt && cat /tmp/mxc_combined/result.txt; then echo '  SUCCESS: Write and read worked'; else echo '  ERROR: Write or read failed'; rc=1; fi; echo ''; echo 'Test 2: Write to denied path (/Users)...'; if touch /Users/forbidden.txt 2>/dev/null; then echo '  ERROR: Should not have succeeded!'; rc=1; else echo '  SUCCESS: Access denied'; fi; echo ''; echo 'NETWORK TESTS (open):'; echo '------------------------------------------------------------'; echo 'defaultPolicy is allow, so outbound traffic is unrestricted. Seatbelt has'; echo 'no per-host filtering, so allow is all-or-nothing -- see 19 for the deny side.'; echo 'These two tests need working internet access.'; echo ''; echo 'Test 3: Connecting to api.github.com...'; if curl -s --max-time 5 https://api.github.com/zen > /dev/null 2>&1; then echo '  SUCCESS: Connection allowed'; else echo '  ERROR: Connection failed (expected success -- needs internet access)'; rc=1; fi; echo ''; echo 'Test 4: Connecting to example.com...'; if curl -s --max-time 5 https://example.com > /dev/null 2>&1; then echo '  SUCCESS: Connection allowed'; else echo '  ERROR: Connection failed (expected success -- needs internet access)'; rc=1; fi; echo ''; echo '============================================================'; if [ $rc -eq 0 ]; then echo 'Combined test complete!'; else echo 'Combined test FAILED'; fi; exit $rc",
        "timeout": 45000
    },
    "filesystem": {
        "readwritePaths": [
            "/tmp"
        ],
        "deniedPaths": [
            "/Users"
        ]
    },
    "network": {
        "defaultPolicy": "allow"
    }
}
