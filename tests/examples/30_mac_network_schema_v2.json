{
    "$schema": "../../schemas/dev/mxc-config.schema.0.8.0-dev.json",
    "version": "0.8.0-alpha",
    "containment": "seatbelt",
    "process": {
        "commandLine": "echo 'Testing schema-0.8 network policy...' && echo '' && echo 'Test 1: Connecting through the loopback proxy...' && curl -s --max-time 5 -x http://127.0.0.1:8080 https://api.github.com/zen && echo '' && echo '  SUCCESS' && echo '' && echo 'Test 2: Connecting directly (should be blocked, egress.default is deny)...' && curl -s --max-time 5 --noproxy '*' https://example.com > /dev/null 2>&1 && echo '  ERROR: Should not have connected!' || echo '  SUCCESS: Direct connection blocked as expected' && echo '' && echo 'Schema-0.8 network policy test complete!'",
        "timeout": 30000
    },
    "filesystem": {
        "readwritePaths": [
            "/tmp"
        ]
    },
    "network": {
        "egress": {
            "default": "deny"
        },
        "ingress": {
            "default": "deny",
            "hostLoopback": "deny"
        }
    },
    "runtimeConfig": {
        "networkProxy": "http://127.0.0.1:8080"
    }
}
